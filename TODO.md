# Backlog

Ideas captured but not yet designed or planned. Pull one into a proper
spec/plan (see `docs/superpowers/`) before implementing.

## Manual Notes from David:
- User should be able to add custom fields per client or per matter.  Some clients require "Phase" and some require "Task" with specific format.  User can establish custom fields for these and others and even add dropdown values per field.
  — spec'd + planned 2026-07-15 (⚠️ assumptions flagged in spec — worth a skim) → `docs/superpowers/specs/2026-07-15-custom-fields-design.md`, `docs/superpowers/plans/2026-07-15-custom-fields.md`

## UI feedback (screenshots)

Captured in-app with Alt+drag. Address the item, then DELETE the
referenced screenshot (see CLAUDE.md).
