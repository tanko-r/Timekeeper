# Backlog

Notes and feedback for `/todo` to work — every item here, however big, is an
active task to build end to end, not an idea to defer. `/todo` only pauses to
ask David a question when the codebase itself can't answer it.

## Manual Notes from David:

## UI feedback (screenshots)

Captured in-app with Alt+drag. Address the item, then DELETE the
referenced screenshot (see CLAUDE.md).
- [ ] 2026-09-14 08:59 — I need a quick-add matter button.  Field for CM#, Client Name, Matter name.  CM# will be numbers pasted in as "######-######" or "###### ######" or "######.######" or really any combination of characters that includes two numbers of six digits each, including a lengthy paragraph, and the number should be automatically parsed.  IF the client exists, name is prefilled.  User fills in matter name, and Billable/Non (default billable) and task/notask (default no task).  User selects grouping. (no screenshot · #/)
