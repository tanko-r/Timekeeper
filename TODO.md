# Backlog

Ideas captured but not yet designed or planned. Pull one into a proper
spec/plan (see `docs/superpowers/`) before implementing.

## Manual Notes from David:

## UI feedback (screenshots)

Captured in-app with Alt+drag. Address the item, then DELETE the
referenced screenshot (see CLAUDE.md).
- [ ] 2026-08-27 14:08 — when a timer is stopped from the start/stop button in the entries pane, it should also pop up the narrative suggestions same as in a timer field. (no screenshot · #/)
- [ ] 2026-08-30 10:55 — Ghost text predictions in narratives should be preloaded with matter-derived snippets. If the matter has several entries mentioning "development agreement" the ghost text should predict a given action was done with that document. E.g. user starts typing "review and analyze" and ghost text autofills "Development Agreement". Or people get auto filled e.g. "email with..." Pops up "A. Hessburg" and then "regarding Development Agreement". Basically the AI model guesses a number of scenarios, documents, people, entities etc and adds them to the prediction engine. (no screenshot · #/)
